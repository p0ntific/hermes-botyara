version: '3.8'
services:
  hermes-bot:
    build: .
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./accounts.yaml:/app/accounts.yaml:ro
    environment:
      - HERMES_DB=/app/data/hermes.db
      - DB_FILE=/app/data/mtproto_leads.json
